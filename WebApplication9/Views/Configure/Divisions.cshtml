@model IEnumerable<WebApplication9.Data.Division>
@Styles.Render("~/Content/datatables")

<table class="data-table display" id="data-table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name) 
            </th>
            <th class="datatable-nosort"></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "EditDivision", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="configure-control">
    <a href="@Url.Action("CreateDivision")">
        <span>
            <span class="btn-mnu btn-default">New Division</span>
        </span>
    </a>

    <a href="@Url.Action("Index")">
        <span>
            <span class="btn-mnu btn-custom">Back To List</span>
        </span>
    </a>
</div>

@Scripts.Render("~/bundles/datatables")

<script>
    $(document).ready(function () {
        $('#data-table').DataTable({
            columnDefs: [{
                targets: "datatable-nosort",
                orderable: false
            }]
        });
    });
</script>